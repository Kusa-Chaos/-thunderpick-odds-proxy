name: Thunderpick Datacenter Diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Explain status
        run: echo 'Direct GitHub-hosted Chromium is kept manual-only because Thunderpick Cloudflare returns HTTP 403 to GitHub datacenter IPs.'
